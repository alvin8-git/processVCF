# Data files (runtime generated)
*.vcf
*.xlsx
data/

# Demo data (downloaded from GitHub Releases)
DemoData/
DemoData.tar.gz

# Build artifacts
*.log

# Large example files (not needed for Docker)
annovar/example/gene_fullxref.txt

# VEP test files (not needed for Docker build)
ensembl-vep/t/
ensembl-vep/travisci/
ensembl-vep/*.txt
ensembl-vep/htslib/test/*.txt
